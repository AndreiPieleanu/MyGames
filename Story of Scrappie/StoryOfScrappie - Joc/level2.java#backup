import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class level2 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class level2 extends LockUnlock
{
    /**
     * Act - do whatever the level2 wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public level2(){
        setImage(new GreenfootImage("Nivelul 2", 25, 
        Color.BLACK, Color.WHITE));
    }
    public void act() 
    {
        if(Greenfoot.mousePressed(this)){
            SelectLevel.levelSituation = 2;
            Greenfoot.setWorld(new Nivel2());
        }
    }    
}
