import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class FinishedLevel here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class FinishedLevel extends World
{

    /**
     * Constructor for objects of class FinishedLevel.
     * 
     */
    public static final GreenfootSound victory = new GreenfootSound("victory.mp3");
    public FinishedLevel()
    {    
        // Create a new world with 600x400 cells with a cell size of 1x1 pixels.
        super(900, 600, 1); 

        prepare();
    }

    /**
     * Prepare the world for the start of the program.
     * That is: create the initial objects and add them to the world.
     */
    private void prepare()
    {
        /*StartWorld.menuMusic.stop();
        Defeat.defeat.stop();
        ShopBackground.shopMusic.stop();
        if(boatProperties.muzicaVictorie)
            victory.play();*/
        Mesaje mesaje = new Mesaje("Inapoi la harta", 30);
        addObject(mesaje,449,542);
    }
}
